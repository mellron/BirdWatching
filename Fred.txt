using System;
using System.Web;

public static class NavigationUtility
{
    public static bool DidUserUseDefault()
    {
        // Check if the session variable is set
        if (HttpContext.Current.Session["MenuClicked"] != null && (bool)HttpContext.Current.Session["MenuClicked"])
        {
            return true;
        }
        return false;
    }
}

